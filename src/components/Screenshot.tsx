import React from "react";
import domtoimage from "dom-to-image";

interface Props {}

const Screenshot = (props: Props) => {


  return <button onClick={() => console.log("Screenshot")}>Generate</button>;
};

export default Screenshot;
